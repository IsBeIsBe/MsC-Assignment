package commandline;

public class Card {
	/*
	 * Card class, which will be stored within a static array of 
	 * 40 cards per deck. Contains a name, and a consistent selection
	 * 5 attributes
	 */
	String name;
	int[] attributes = new int[5];
}
